__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
.pytest_cache/
.ruff_cache/
build/
dist/
.coverage
*.swp
.DS_Store

# Lambda packaging output
function.zip

# Terraform local state and provider plugins (not state — that's in S3)
terraform/.terraform/
terraform/.terraform.tfstate.lock.info
terraform/*.tfstate
terraform/*.tfstate.backup
terraform/*.tfvars.local

# Kenning lexicon DB — build artifact, regenerable via
# `wyrd kenning lexicon build`
wyrd/generators/kenning/data/lexicon.db

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
